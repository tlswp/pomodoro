export enum TimerState {
  STOPPED = 'STOPPED',
  PAUSED = 'PAUSED',
  PLAYING = 'PLAYING',
  FINISHED = 'FINISHED',
}

export enum TimerType {
  SESSION = 'SESSION',
  LONG_BREAK = 'LONG_BREAK',
  SHORT_BREAK = 'SHORT_BREAK',
}
